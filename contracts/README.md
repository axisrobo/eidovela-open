# Contract Governance

Each version directory contains the public schemas and protocol profiles for that contract line. `v1alpha1` is pre-stable but still must preserve compatibility for published consumers.

Core implementation is AGPL and lives in `eidovela`; these contracts remain Apache-2.0 and dependency-free from core/EE.
